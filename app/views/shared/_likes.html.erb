<% if user_signed_in?
	<% if current_user.likes?(@box) %>
		<%= link_to "Unlike", box_like_path(@box), method: :delete %>
	<% else %>
		<%= link_to "Like", box_like_path(@box), method: :post %>
	<% end %>
<% end %>

<%= @box.likes.count %> users like this